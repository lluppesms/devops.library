#
# PSRule for Azure configuration
#

# Please see the documentation for all configuration options:
# https://aka.ms/ps-rule/options
# https://aka.ms/ps-rule-azure/options

# Configure binding for local rules.
binding:
  preferTargetInfo: true
  targetType:
  - type
  - resourceType

# Require minimum versions of modules.
requires:
  PSRule: '@pre >=2.4.0'
  PSRule.Rules.Azure: '@pre >=1.19.2'

# Use PSRule for Azure.
include:
  module:
  - PSRule.Rules.Azure

output:
  culture:
  - 'en-US'

input:
  pathIgnore:

  # Ignore other files in the repository.
  - '.vscode/'
  - '.github/'
  - '*.md'
  - '*.yml'

  # Exclude modules but not tests.
  - 'bicep/modules/**/*.bicep'
  - '!bicep/modules/**/*.tests.bicep'

execution:
  notProcessedWarning: false

configuration:
  # Enable automatic expansion of Azure parameter files.
  AZURE_PARAMETER_FILE_EXPANSION: true

  # Enable automatic expansion of Azure Bicep source files.
  AZURE_BICEP_FILE_EXPANSION: true

  # Configures the number of seconds to wait for build Bicep files.
  AZURE_BICEP_FILE_EXPANSION_TIMEOUT: 10

# Suppression ignores rules for a specific Azure resource by name.
suppression:
  Azure.AppService.HTTP2:
    - Bicep/website.bicep
  Azure.AppService.AlwaysOn:
    - Bicep/website.bicep
  Azure.KeyVault.SecretName:
    - Bicep/keyvaultsecretcosmosconnection.bicep
    - Bicep/keyvaultsecretiothubconnection.bicep
    - Bicep/keyvaultsecretservicebusconnection.bicep
    - Bicep/keyvaultsecretsignalrconnection.bicep
    - Bicep/keyvaultsecretstorageconnection.bicep